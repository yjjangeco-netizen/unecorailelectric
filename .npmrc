# Enforce strict peer dependency resolution
strict-peer-deps=true

# Use exact package-lock.json versions
# package-lock-only=true

# Fail on audit vulnerabilities during install
audit-level=moderate

# Engine-strict enforcement
engine-strict=true